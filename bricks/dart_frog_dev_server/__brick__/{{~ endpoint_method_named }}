/// The '{{{path}}}' endpoint.
Endpoint {{name.camelCase()}}({ {{#params}}required String {{.}},{{/params}} }) {    
  final uri = Uri.parse('$_uri$_path{{{path}}}');
  return Endpoint(uri, _client);
}