name: ci

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/semantic_pull_request.yml@v1

  spell-check:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/spell_check.yml@06ced12749bd8de77eeb1cfbc450fa66833c7a3d
    with:
      verbose: true

  pana:
    runs-on: ubuntu-latest
    steps:
      - name: noop
        run: echo 'noop'
