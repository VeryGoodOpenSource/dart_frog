name: ci

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/semantic_pull_request.yml@v1

  spell-check:
    uses: VeryGoodOpenSource/very_good_workflows/.github/workflows/spell_check.yml@c8fdb55d7a2ed1a80279dfcbf93664421663852c
    with:
      includes: |
        **/*.md
      modified_files_only: false
      verbose: true

  pana:
    runs-on: ubuntu-latest
    steps:
      - name: noop
        run: echo 'noop'
